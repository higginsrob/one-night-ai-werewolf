/**
 * GameEngine is the composition root for scene + active game plugin.
 * Networking stays outside; this module only mounts presentation.
 */
export { PeerScene as GameEngine } from '../scene/PeerScene'
