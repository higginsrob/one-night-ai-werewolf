/**
 * Stub — Trackball controls adapter for future games.
 * Falls back to orbit-like behavior via documentation only in M1.
 */
export function TrackballControlsAdapter(_props: {
  enabled?: boolean
  target?: [number, number, number]
}) {
  return null
}
